/**
 * Shared domain types for MULTIPLICA foundation.
 * Pastoral process types are added in later phases.
 */

export type NetworkCode = "hombres" | "mujeres" | "jovenes" | "ninos";

export type AssignmentScope = "global" | "ministry" | "network" | "tree";

export type Uuid = string;
