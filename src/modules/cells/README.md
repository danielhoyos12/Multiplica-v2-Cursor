# Células

Núcleo operativo Phase 3:

- `cells` — evangelistic | twelve
- `cell_memberships` — historial; referencia `person_id` (GANAR)
- `cell_attendance_sessions` / `cell_attendance`

Compatibilidad de Red vía pertenencia actual en `person_organization_history` (sin inferir género).

Fuera de alcance: activación de líderes, árbol G12, conversión real a Célula de 12.
